{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "opus",
  "alwaysThinkingEnabled": true,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash|Read",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,os; data=json.load(sys.stdin); cmd=data.get('tool_name','unknown'); tokens_est=100+len(str(data.get('tool_input',{})))*0.1; os.system(f'python3 .claude/track_operation.py \\\"{cmd}\\\" {int(tokens_est)} {int(tokens_est//2)}')\" 2>/dev/null || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
